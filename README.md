# Git Flow Basico
comando para el flujo basico 
```sh
git status
git add . o git -a
git commit -m "Descripcion del cambio del proyecto"
git push
```
# nota:
Si no me dice quien soy, entonces necesito restaurar la PC.
